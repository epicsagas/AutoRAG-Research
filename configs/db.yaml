# Database connection configuration
host: localhost
port: ${oc.env:POSTGRES_PORT,5432}
user: ${oc.env:POSTGRES_USER,postgres}
password: ${oc.env:POSTGRES_PASSWORD,postgres} # Recommend not changing and use POSTGRES_PASSWORD environment variables
